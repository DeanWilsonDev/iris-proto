build/
.DS_Store
compile_commands.json

# iris-lsp scratch: the generated C++ view of an open .iris buffer it hands to clangd
# (docs/iris_lsp_decision.md) — regenerated on every edit, never meant to be committed.
*.iris.generated.h
*.irisx.generated.h
